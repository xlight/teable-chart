# Patterns to ignore when building packages.
# This supports shell glob matching, relative path matching, and
# negation (prefixed with !). Only one pattern per line.
.DS_Store
# Common VCS dirs
.git/
.gitignore
.bzr/
.bzrignore
.hg/
.hgignore
.svn/
# Common backup files
*.swp
*.bak
*.tmp
*.orig
*~
# Various IDEs
.project
.idea/
*.tmproj
.vscode/
# OS generated files
Thumbs.db
# Helm ignore patterns
charts/*.tgz
*.lock
# Test files
test/
tests/
# Documentation (except Chart.yaml and values.yaml)
*.md
!Chart.yaml
!values*.yaml
# CI/CD files
.github/
.gitlab-ci.yml
.travis.yml
Jenkinsfile
# Development files
docker-compose*.yml
Dockerfile*
Makefile
# Temporary Helm files
requirements.lock
Chart.lock
